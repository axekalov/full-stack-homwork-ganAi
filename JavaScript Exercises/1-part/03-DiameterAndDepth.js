const diametr =  Math.floor(Math.random() * 100)
const depth =  Math.floor(Math.random() * 100)
const r = diametr/2
const area = Math.PI * Math.pow(r,2)
const capacity = area * depth
console.log(`the radus is:${r}`);
console.log(`the area is:${area}`);
console.log(`the capacity is:${capacity}`);

